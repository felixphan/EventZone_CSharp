@model EventZone.Models.ViewDetailEventModel
@using EventZone.Models
@using (Html.BeginForm("EditEvent", "Event"))
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        @Html.ValidationSummary(true)
        @Html.HiddenFor(model => model.eventId)
        <div class="form-group">
            <p class="control-label col-md-2">Event Name</p>
            <div class="col-md-10">
                @Html.TextBoxFor(model => model.eventName)
                @Html.ValidationMessageFor(model => model.eventName)
            </div>
        </div>

        <div class="form-group">
            <p class="control-label col-md-2">Location</p>

            @Html.Partial("_LocationPartial",Model.eventLocation)
            @*@if (Model.eventLocation.Count != 0)
                {
                    <div class="col-md-10">
                        @for (int i = 0; i < Model.eventLocation.Count; i++)
                        {
                        @Html.EditorFor(m => m.eventLocation[i].LocationName)
                        @Html.ActionLink("Delete","DeleteLocation", "Event", new { id= Model.eventLocation[i].LocationID})
                            <br></br>
                        }
                </div>


    }*@

        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </div>
    </div>
}


    

